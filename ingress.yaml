apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: k8s-ml-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: rke2-server
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: k8s-ml-svc
            port:
              number: 80
